#include <iostream>
using namespace std;
int main()
{
  const int num=1000;
  double value[num];
  int array;

  cout << "Please enter an integer from 0-999: "; 
  for(array=0;array<1000;array++)
    {
      value[array]=array*7.5;
    }
 cin >> array;
 if (0<=array && 999>=array)
   {
  cout << "The value in array slot " << array << " is " << value[array] << endl;
   }
 else
   {
     cout << "This value is out of range." << endl;
   }  
return 0;
}
